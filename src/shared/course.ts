/**
 * Course module: re-exports the course data, palette and catalog.
 * The data itself lives in `sunrise-scramble.ts`, the colour constants in
 * `course-palette.ts`, the authoring builders in `course-builders.ts` and the
 * catalog in `course-catalog.ts`.
 */

export { COURSES } from "./course-catalog"
export { PALETTE, RUNNER_COLORS } from "./course-palette"
export { SUNRISE_SCRAMBLE } from "./sunrise-scramble"
